{% from "partials/icon.njk" import icon %}

{% macro card(props) %}
  <a href="{{ props.url }}" class="card">
    {% if props.stars and props.forks %}
      <dl class="github-stats">
        <dt>{{ icon({ id: "star", title: "Star" }) }} Stars</dt>
        <dd>{{ props.stars }}</dd>
        <dt>{{ icon({ id: "fork", title: "Fork" }) }} Forks</dt>
        <dd>{{ props.forks }}</dd>
      </dl>
    {% endif %}
    <h3 class="card__title">{{ props.title }}</h3>
    <p class="card__description">{{ props.description }}</p>
    <small class="card__details">{{ props.details }}</small>
  </a>
{% endmacro %}
